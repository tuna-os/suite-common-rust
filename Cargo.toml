[package]
name = "suite-common-rs"
version = "0.1.0"
edition = "2021"
description = "Shared GTK4/libadwaita scaffold for the tuna-os office suite (Rust)"

[dependencies]
gtk4 = { version = "0.9", package = "gtk4" }
libadwaita = "0.7"

[lib]
name = "suite_common_rs"
path = "src/lib.rs"
